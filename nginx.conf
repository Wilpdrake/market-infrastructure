events {
}

http {
    server {
        listen 80;
        
        #location / {
        #    root /usr/share/nginx/html;
        #    index index.html;
        #}

        location / {
            proxy_pass http://frontend:7000;
        }
    }
}
